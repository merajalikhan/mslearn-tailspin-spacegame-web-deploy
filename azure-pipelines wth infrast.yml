trigger:
- '*'

variables:
  buildConfiguration: 'Release'

stages:
- stage: 'Build'
  displayName: 'Build the web application'
  jobs: 
  - job: 'Build'
    displayName: 'Build job'
    pool:
      vmImage: 'ubuntu-20.04'
      demands:
      - npm

    variables:
      wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
      dotnetSdkVersion: '6.x'

    steps:
    - task: UseDotNet@2
      displayName: 'Use .NET SDK $(dotnetSdkVersion)'
      inputs:
        version: '$(dotnetSdkVersion)'

    - task: Npm@1
      displayName: 'Run npm install'
      inputs:
        verbose: false

    - script: './node_modules/.bin/node-sass $(wwwrootDir) --output $(wwwrootDir)'
      displayName: 'Compile Sass assets'

    - task: gulp@1
      displayName: 'Run gulp tasks'

    - script: 'echo "$(Build.DefinitionName), $(Build.BuildId), $(Build.BuildNumber)" > buildinfo.txt'
      displayName: 'Write build info'
      workingDirectory: $(wwwrootDir)

    - task: DotNetCoreCLI@2
      displayName: 'Restore project dependencies'
      inputs:
        command: 'restore'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build the project - $(buildConfiguration)'
      inputs:
        command: 'build'
        arguments: '--no-restore --configuration $(buildConfiguration)'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Publish the project - $(buildConfiguration)'
      inputs:
        command: 'publish'
        projects: '**/*.csproj'
        publishWebProjects: false
        arguments: '--no-build --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/$(buildConfiguration)'
        zipAfterPublish: true

    - publish: '$(Build.ArtifactStagingDirectory)'
      artifact: drop

- stage: 'Provision'
  displayName: 'Create Infrastructure'
  #dependsOn: Build    
  jobs: 
    - job: 'Provision'
      displayName: 'Provision job'
      pool:
        vmImage: 'ubuntu-20.04'
      steps:
        - checkout: self
        #- task: AzureCLI@2
        #  displayName: Create VM using az cli
        #  inputs:
        #    azureSubscription: 'MAKHANX'
        #    scriptType: 'bash'
        #    scriptLocation: 'inlineScript'
        #    inlineScript: |
        #      az vm create \
        #          --name MKTESTVN1 \
        #          --resource-group mk-space-game-rg \
        #          --image canonical:0001-com-ubuntu-server-focal:20_04-lts:latest \
        #          --size Standard_DS2_v2 \
        #          --admin-username azureuser \
        #          --generate-ssh-keys
        #      sudo apt-get update   
        #      curl https://raw.githubusercontent.com/MicrosoftDocs/mslearn-tailspin-spacegame-web/main/.agent-tools/build-tools.sh > build-tools.sh
        #      cat build-tools.sh

        #- task: AzureCLI@2
        #  displayName: Create app resource group using cli
        #  inputs:
        #    azureSubscription: 'MAKHANX'
        #    scriptType: 'bash'
        #    scriptLocation: 'inlineScript'
        #    inlineScript: |
        #        az group create 
        #          --name mk-space-game-rg

        #- task: AzureCLI@2
        #  displayName: Create app service plan using cli
        #  inputs:
        #    azureSubscription: 'MAKHANX'
        #    scriptType: 'bash'
        #    scriptLocation: 'inlineScript'
        #    inlineScript: |
        #        az appservice plan create \
        #          --name mk-space-game-asp \
        #          --resource-group mk-space-game-rg \
        #          --sku F1 \
        #          --is-linux
        
        - task: AzureCLI@2
          displayName: Create app web app using cli
          inputs:
            azureSubscription: 'MAKHANX'
            scriptType: 'bash'
            scriptLocation: 'inlineScript'
            inlineScript: |
                az webapp create \
                    --name mk-space-game-786 \
                    --resource-group mk-space-game-rg \
                    --plan mk-space-game-asp \
                    --runtime "DOTNET|6.0"
                  

- stage: 'Deploy'
  displayName: 'Deploy the web application'
  dependsOn: Provision
  jobs:
  - deployment: Deploy
    pool:
      vmImage: 'ubuntu-20.04'
    environment: dev
    variables:
    - group: Release
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: drop
          - task: AzureWebApp@1
            displayName: 'Azure App Service Deploy: website'
            inputs:
              azureSubscription: 'Resource Manager - MK- Space Game'
              appName: '$(WebAppName)'
              package: '$(Pipeline.Workspace)/drop/$(buildConfiguration)/*.zip'